# AGENTS.md — 工具站（Utility-Site）Agent 总纲

> 本文件是工具站的**唯一通用规范**：既说明仓库怎么组织，也约束 Agent 的需求分析、实施、验收与交付行为。
>
> 动手改任何东西之前，先读完本文件；如果任务是针对某个子项目的，再读那个子项目自己的 `AGENTS.md`。

---

# 第一部分　工具站本身

## 1. 这是什么

工具站是一个 **monorepo**：在 git 层面它是**一个**仓库，在内容上它由许多**互相独立的小工具**组成。

每个工具占据根目录下的**一个文件夹**，是一个能独立使用的小项目。它们共享同一份 git 历史，但不共享代码、不共享依赖、不共享构建流程 —— 一个子项目坏掉了，不应该影响其他任何一个。

一句话概括：**一个仓库，装很多个互不相干的小工具。**

## 2. 目录结构

根目录**只放三份文档**，其余全部是子项目文件夹：

```text
Utility-Site/
├── AGENTS.md          ← 本文件：唯一的通用规范，根级
├── README.md          ← 给人看的门面：这站是什么、有哪些工具
├── .gitignore         ← 全站共用的忽略规则
├── .github/           ← CI/CD 流水线（唯一经用户批准的结构例外，见 21.2）
├── <子项目-a>/         ← 每个子项目一个文件夹
├── <子项目-b>/
└── ...
```

根目录**不建** `constitution/`、`specs/`、`.ai/`。这套 Agent 结构下沉到**每个子项目内部**（见第 6 节）。
根目录也不放任何子项目的代码、资源或配置文件。
`.github/` 是**唯一例外**（2026-09-16 用户批准）：GitHub Actions 的 workflow
只能放在仓库根目录，没有别的合法位置。

### 2.1 两层结构：项目结构 vs Agent 结构

```text
项目结构   负责「项目怎么组织」—— 业务代码、前端、后端、tests、docs、构建脚本……
Agent 结构 负责「Agent 怎么开发这个项目」—— AGENTS.md、constitution/、specs/、.ai/
```

两者是不同的问题，不要混在一起谈。本规范对两者都有约束，但约束方式不同：项目结构跟随技术栈走，Agent 结构是固定的框架。

## 3. 子项目的独立性

- 子项目之间**不互相引用**：不共享组件、不共享工具函数、不从别的子项目 import 任何东西。
- 出现重复代码时**先复制**，不要急着抽公共库。等同样的东西在**第三个**子项目里再出现，才考虑抽共享层 —— 到那时必须单独讨论，不允许自作主张地建 `shared/`、`common/` 之类的目录。
- **默认只动当前任务相关的那个子项目文件夹。** 改完它，任务就算完成。
- **绝对不要顺手「优化」其他子项目。** 看到别的文件夹里有问题，报告出来，不要直接改。
- 不要为了让自己的改动通过，去修改其他子项目或全站配置。
- 需要动根目录文件（本文件、README.md、.gitignore）时，先说明为什么，再动手。

## 4. 命名与登记

文件夹名用小写英文 + 连字符（kebab-case），例如 `voice-collector`、`image-compressor`。不用中文、不用空格、不用下划线、不用驼峰。名字要能一眼看出这个工具干什么。

每个子项目都要在**根 `README.md` 的工具清单**和**本文末的「当前子项目」表**里各占一行。新增或改名时两处同步更新。

## 5. 提交规则

- 提交信息用中文，第一行简洁说明做了什么，需要时下面补细节。
- **一个子项目的改动，一次提交。** 不要把几个不相干的子项目混在同一次提交里。
- 新建子项目时，`<子项目>/` 文件夹、它的文档、以及根 README 与本文清单的改动，算作同一次提交。
- **绝不要提交**：依赖目录（`node_modules/` 等）、构建产物（`dist/`、`build/` 等）、密钥与账号凭据、编辑器/系统垃圾文件（`.DS_Store` 等）。

### 5.1 `.gitignore` 怎么维护

**忽略规则统一写在根目录的 `.gitignore`，子项目默认不建自己的。**

`.gitignore` 是分层生效的：根目录那份对它下面所有子项目都管用；不带前导斜杠的写法（如 `node_modules/`、`dist/`）会匹配任意深度的同名目录，所以一句就能覆盖全部子项目，不需要逐个重复。

只有一种情况例外：某条规则**仅对某一个子项目成立**，写进根目录会误伤其他子项目（例如 A 项目生成 `output/` 需要忽略，而 B 项目里的 `output/` 恰好是要提交的源码目录）。这时才在那个子项目内单独建一份 `.gitignore`，并**在文件里注明为什么写在局部而不是根目录**。

除此之外，不要为了「看起来完整」而在子项目里复制一套通用规则。新增规则时加到根 `.gitignore`，并在改动说明里讲一句为什么。

### 5.2 分支与 CI/CD

- **默认在 `dev` 分支开发**，改动提交后 push 到 `dev`。
- **push 到 `dev` 不触发 CI**；CI 只在 PR 时执行。
- 开发完成后由用户在 GitHub 手动发起 PR，Agent 不自行创建 PR。
- PR 合并完成后，对应子项目若有 CD，可进入 CD 流程。

### 5.3 部署与运维约定

全站部署在**一台服务器**上：每个子项目一个 Docker 容器，共享一个反向代理。
共享设施（反向代理、证书、端口分配、网络、备份）统一归
[`deploy/`](./deploy/) 子项目管理，子项目不得自行占用或修改共享配置。

- **子项目自备可部署单元**：`Dockerfile`、本项目的 `docker-compose.yml`、
  环境变量与数据卷说明，由该子项目的实现者维护；服务器共享设施不由子项目负责。
- **端口先登记再使用**：在 [`deploy/ports.md`](./deploy/ports.md) 登记宿主端口，
  不得与已分配端口冲突。多个子项目并存时**按端口区分**：应用端口仅本机使用，
  需要 HTTPS 的项目由共享代理在对外端口上提供服务（约定与接线流程见
  `deploy/README.md` 与 `deploy/ports.md`）。
- **共享设施只改 `deploy/`**：代理路由、证书、网络等改动都在该子项目内完成。
- **凭据不进仓库**：服务器上的 `.env`、证书与密钥由使用者保管，仓库只放样例与说明。
- **部署命令**：`./deploy/scripts/deploy.sh <子项目名|proxy|all>`，
  在服务器上的仓库根目录执行；当前为手动/半自动部署。
- **CI/CD 流水线**（`.github/workflows/`，2026-09-16 用户批准）：
  - `ci.yml` 在 PR 时执行，只校验本次改动到的子项目（语法检查、
    compose 校验、可部署单元构建），不做全量重验；
  - `cd.yml` 在合并到 `main` 后执行，按改动目录通过 SSH 部署对应子项目；
    服务器凭据未配置时跳过并提示，不失败；
  - 流水线内容属于共享设施，改动时同步维护 `deploy/README.md` 的说明。

---

# 第二部分　工作流分档

## 6. 分档机制

工具站里既有几十行的小工具，也有正经项目，所以流程分两档。每个子项目的默认配置：

```yaml
workflow_tier: AUTO    # AUTO | LIGHT | FULL
project_mode: AUTO     # AUTO | NEW | EXISTING
multi_agent: AUTO      # AUTO | true | false
```

`AUTO` 表示由协调 Agent 按下面的标准自行判断；用户显式指定的，优先于自动判断。

- `project_mode: AUTO` 时：目标是新文件夹 → 按 NEW 处理；目标是已经存在、有代码或文档的文件夹 → 按 EXISTING 处理；**无法可靠判断时按 EXISTING 处理**，以免破坏已有成果。

### 6.1 什么时候可以用 LIGHT 档

同时满足以下**全部**条件，才可以用 LIGHT 档：

- 纯前端 / 纯静态，没有后端服务；
- 不涉及用户数据持久化（无数据库、无账号体系）；
- 不依赖需要密钥或账号的外部服务；
- 规模很小：源码文件大致不超过 10 个；
- 一次会话内可以做完并验证。

任何一条不满足，就升到 FULL 档。**判断不明时按 FULL 处理**，或者直接问用户。

### 6.2 LIGHT 档的要求

LIGHT 档**只需要**：

```text
<子项目名>/
├── README.md      ← 做什么、怎么跑、用什么技术
└── <入口文件>      ← index.html 或同类
```

不需要 `constitution/`、`specs/`、`.ai/`，也不需要独立验收 Agent。

但**仍然**必须遵守全站硬约束：目录边界、子项目独立性、提交规则、安全规则，以及 —— 必须真的把它跑一遍再交付，不得只凭读代码就说「应该没问题」。

### 6.3 FULL 档的要求

FULL 档的子项目内部结构：

```text
<子项目名>/
├── AGENTS.md          ← 简短：声明继承根规范 + 本项目特有信息
├── README.md
├── constitution/
│   ├── mission.md
│   ├── roadmap.md
│   └── tech-stack.md
├── specs/
│   ├── README.md
│   └── _template/
│       ├── spec.md
│       ├── plan.md
│       └── acceptance.md
├── .ai/
│   ├── decisions/
│   ├── workflows/
│   ├── prompts/
│   └── rules/
├── docs/
└── <业务代码目录，由技术栈决定>
```

`tests/` 是否单独建目录由技术栈决定，确定后记进 `constitution/tech-stack.md`。

`AGENTS.md`、`constitution/`、`specs/`、`.ai/` 属于**固定 Agent 结构**：允许扩展，**不允许擅自删除、重命名、移动、合并、用其他目录替代，也不允许另建一套职责重复的治理体系**。

核心原则：**Agent 结构允许扩展，不允许擅自替换。**

### 6.4 中途升级

LIGHT 档的项目一旦越过 6.1 的任何一条（加了后端、要存数据、代码量涨起来……），就应该升级到 FULL 档：补建 Agent 结构，并把已经做过的关键决策补记进 `.ai/decisions/`。升级属于结构性变更，要跟用户说一声。

### 6.5 判档之后：先跟用户确认流程重量

档位判定完成后、动手之前，协调 Agent **必须**先跟用户确认本次走多重，
不得默认按完整闭环开跑。确认时给出：判定的档位与依据、完整闭环预计的轮数与量级、
你的建议（完整闭环还是轻量路径）。

**轻量路径不是"不验证"**：协调 Agent 自己实施并写深度测试
（重点是压力与异常路径 —— 断线、超时、并发、畸形输入、边界值），
再由独立于实施的人或会话复核证据与结论。用户明确要求完整闭环时照走；
用户没有表态时不得默认全开。

## 7. 初始化一个子项目（NEW）

1. **先想清楚，再动手。** 不得在尚未理解产品目标的情况下直接生成大量业务代码。
   判档完成后、动手之前，按 6.5 跟用户确认本次的流程重量。
2. 至少搞清楚：它是什么、解决什么问题、给谁用、核心能力是什么、这一阶段做到什么程度、**明确不做什么**、有没有重要外部约束。用户已经说明过的信息不得重复询问。
3. 建文件夹（kebab-case 命名）。
4. 写 `README.md`。
5. 按档位补齐：LIGHT 档到此为止；FULL 档继续建 `constitution/`，把 mission / roadmap / tech-stack 三份写出来（见第 18 节）。
6. FULL 档：产品目标和技术方向具备实施条件后，创建第一个 Spec（见第 17 节）。
7. 登记进根 `README.md` 的工具清单。
8. **不要先写代码后补文档。** 文档先行不是形式主义 —— 它决定了这个工具的名字、边界和技术选型，写完再动手会少返工。

## 8. 接手一个已有的子项目（EXISTING）

> **已有结构优先，Agent 结构嵌入其中。**

不是让旧项目去适配本规范的默认目录，而是把 Agent 工作流接进旧项目。

接手时先读懂：README、业务代码、依赖清单与 lockfile、构建配置、测试配置、CI、部署配置、现有文档、现有 Agent 规则。

然后**保留**原项目结构，只补齐 Agent 结构：

```text
<子项目>/AGENTS.md
<子项目>/constitution/{mission,roadmap,tech-stack}.md
<子项目>/specs/{README.md,_template/}
<子项目>/.ai/{decisions,workflows,prompts,rules}/
```

这些结构用于**描述和管理**原项目，不用于改变原项目。不得因为本规范有默认的新项目结构，就擅自移动旧代码、重命名旧目录、把旧目录改造成自己偏好的样子、新建职责重复的业务目录、或大规模重构工程结构。

旧项目如果已经存在同类 Agent 文件或规则：先读取 → 保留已有有效约束 → 不直接覆盖 → 与本文做兼容整合 → 存在重大冲突且无法可靠判断时，请用户决策。

旧项目已经使用 `__tests__/`、`documentation/`、`e2e/`、`test/`、`wiki/` 之类的目录时，不得为了与默认结构一致而新建重复目录，只需在 `constitution/tech-stack.md` 里记录真实位置。

为旧项目建立 constitution 时：

- `mission.md`：根据 README、产品文档、现有代码和用户说明总结当前使命，**不得凭空创造不存在的产品目标**；
- `roadmap.md`：有现成路线图就引用整理，依据不足时记录已知阶段和待确认内容，**不得擅自替用户规划长期方向**；
- `tech-stack.md`：必须描述**实际存在**的项目 —— 当前技术栈、业务代码目录、测试位置、文档位置、构建方式、运行方式、依赖与部署方式。它的作用是建立 Agent 对旧项目的可靠认知，不是重新设计旧项目。

---

# 第三部分　Agent 协作规范

## 9. Agent 角色

| 角色 | 定位 | 职责 |
|---|---|---|
| **协调开发 Agent** | 主 Agent | 与用户沟通、识别档位与项目状态、理解现有项目、处理决策、编写和维护 Spec、调度和管理子 Agent |
| **实施 Agent** | 子 Agent | 按已确认的 Spec 和项目约束完成具体开发任务 |
| **验收 Agent** | 子 Agent | 根据 Spec 独立检查和验收实施结果 |

在支持多 Agent 的环境中，优先采用以上角色分工。如果当前运行环境不支持子 Agent，允许降级为单 Agent 工作流，但必须保持「规划 → 实施 → 二次审查 → 验证」的阶段隔离，**不得伪造独立 Agent 验收**。

## 10. 总体协作流程

1. 协调 Agent 判断当前档位（LIGHT / FULL）和项目状态（NEW / EXISTING）。
2. 按对应规则完成项目理解，或把 Agent 结构接入已有项目。
3. 与用户沟通需求、读取项目现状、起草并完善 Spec。
4. 影响实施结果的关键决策确认后，把 Spec 标记为可实施状态。
5. 创建实施 Agent，按 Spec 实施。
6. 实施 Agent 返回 Implementation Report。
7. 创建**新的独立验收 Agent** 进行验收。
8. 验收未通过 → 根据验收结果创建新的实施 Agent 返工 → 再由新的验收 Agent 重新验收。
9. 重复实施 / 验收 / 返工，直到验收通过。
10. 协调 Agent 汇总结果，交用户审查。

```text
需求
↓
档位与状态判断
↓
Spec
↓
实施 Agent → 实施报告
↓
独立验收 Agent → 验收报告
↓
PASS ──────────────→ 最终交付
│
FAIL
↓
返工任务 → 新的实施 Agent → 新的验收 Agent → 重新验收
```

**实施 Agent 与验收 Agent 必须相互独立。验收 Agent 不得代替实施 Agent 修改业务代码；实施 Agent 不负责最终验收。**

LIGHT 档可以跳过 5–9 的独立 Agent 环节，但「实施 → 自己复查 → 实际跑一遍验证」的阶段隔离仍然要做。

### 10.1 独立验收的成本控制

**「独立」指判断独立于实施者，不是把实施阶段重做一遍。**

验收 Agent 集中精力在实施者不易自测的地方：审查证据（测试输出、日志、实测数字）、
设计反例（畸形输入、并发、中断、超时、边界值）、抽查重跑关键路径、逐条核对 Spec。

环境、测试素材、脚本属**可复用资产**，由协调 Agent 在交接时提供，不必每轮重建。
协调 Agent 应当维护一套**测试脚手架**（启动器、登录 / 录音 / 提交等 helper、
固定数据集、一条命令可跑的烟雾套件、环境速查），验收 Agent **只写断言与反例**，
不重建脚手架；脚手架本身不作为验收结论的依据——独立的是判断与反例，不是脚手架。

**验收范围默认收窄**：以「本次变更 diff + Spec 对应条目 + 高风险回归」为界；
逐条全量核对只在里程碑式改动或用户明确要求时做。
**第二轮复验直接复用第一轮的脚本与素材**，只补返工项的反例，不再重造。
协调 Agent 应给每轮验收设定时间预算；发现验收 Agent 长时间滞留于环境搭建或
重复探索时，及时介入补齐资产，而不是让它空耗。

验收标准不降：必须实际执行、必须有证据，结果只能是 PASS / FAIL，
不得用「未执行」冒充「已通过」。

## 11. 协调 Agent 职责边界

协调 Agent 负责：与用户沟通；判断档位与项目状态；理解项目现状；识别需要用户决策的问题；起草、完善和维护 Spec；管理重要决策；创建和控制子 Agent；向子 Agent 完整传递上下文；组织返工；汇总结果；维护 Agent 结构；恢复中断任务状态。

在多 Agent 环境中，协调 Agent 原则上**不直接编写或修改主要业务代码**。它可以：阅读业务代码、搜索项目、分析 git diff、阅读测试、维护 `constitution/` `specs/` `.ai/`、创建任务交接、分析子 Agent 结果。业务实现应交由实施 Agent。

当前环境不支持子 Agent，或用户明确要求主 Agent 直接开发时，允许协调 Agent 降级执行实施工作。

## 12. 子 Agent 硬性约束

- 禁止子 Agent 再创建子 Agent，除非用户或更高层规则明确允许。
- 给予子 Agent 足够思考和执行时间。
- 禁止因单次等待超时就擅自终止仍在正常运行的子 Agent。
- 对仍有明确进展、仍在正常工作的子 Agent 不得擅自关闭；**无任何有效进展且持续空转超过 1 小时**，才可考虑关闭并重新分配任务。
- 子 Agent 任务完成且结果已处理后，应及时释放或删除。
- 一个任务如果涉及多个高度耦合模块的修改，默认只派给**一个**实施 Agent 串行实施。
- 实施 Agent 不做最终验收；验收 Agent 不修改业务代码。
- 子 Agent 不得绕过协调 Agent 替用户做重大项目决策。

## 13. 子 Agent 等待与消息闭环

创建子 Agent 后，协调 Agent 必须保持当前任务活动状态，并使用当前运行环境提供的 `wait_agent`、`list_agents`、status、receive、polling 或等价能力，持续等待和检查子 Agent。

- 单次等待超时 → 只代表需要继续等待；
- 暂无消息 → 不代表完成；
- Agent 仍在运行 → 不代表失败；
- Agent 已完成但报告未读取 → 任务仍未闭环。

存在仍在运行的子 Agent 时，协调 Agent **不得发送任务完成式最终回复**。
存在已返回但尚未读取和处理的 Agent 报告时，协调 Agent **不得发送任务完成式最终回复**。

进度通知只能作为中间状态消息；最终回复是任务闭环动作，不得用于「已创建 Agent，请稍后等待」之类的未完成状态。

## 14. 并行实施规则

默认：

```yaml
allow_parallel_implementation: false
```

一个任务涉及多个模块但彼此高度耦合时，应由**一个**实施 Agent 串行完成。

只有同时满足以下全部条件，才考虑并行：

1. 子任务真正独立；
2. 修改文件不重叠；
3. 接口边界已经确认；
4. 数据结构已经确认；
5. 合并顺序明确；
6. 协调 Agent 可以统一验收。

否则禁止为了追求速度而并行创建多个实施 Agent 去修改项目的同一片区域。

---

# 第四部分　Spec 与决策

## 15. Spec 的目的与关键决策

Spec 用来定义：**要实现什么，以及怎样才算正确完成。** 它不只是一份代码修改清单。

每次起草新 Spec 时，协调 Agent 必须识别会影响实施结果的关键决策，包括：需求范围、业务规则、用户可见行为、数据模型、API 行为、权限和安全、外部服务、兼容性、不可逆操作、可能产生费用的技术选择、验收标准。

关键问题未确认前，不得让实施 Agent 擅自开始存在重大歧义的实施。但**不得为了「走流程」机械提问**；用户已经明确说明的信息，禁止重复询问。

## 16. 决策优先顺序

遇到不明确之处时，按此顺序判断：

```text
用户当前明确说明
↓
根目录 AGENTS.md（本文件）
↓
子项目 AGENTS.md
↓
子项目 constitution/
↓
子项目 .ai/rules/
↓
当前 Spec
↓
已有 Decision
↓
项目现有行为和代码事实
↓
可靠工程惯例
↓
询问用户
```

能从项目可靠判断的工程细节，应根据项目现状自行判断。涉及重大产品决策、不可逆行为、或明显存在多个合理方向的问题，应交给用户决定。

## 17. Spec 目录与规范

Spec 统一放在**子项目内部**的 `specs/`：

```text
<子项目>/specs/
├── README.md
├── _template/
└── spec-001-short-name/
    ├── spec.md
    ├── plan.md
    ├── acceptance.md
    └── rework.md        # 可选
```

命名格式为 `spec-XXX-short-name`。不得擅自把 Spec 改放到 `.ai/specs/`、`docs/specs/` 或其他目录。

### 17.1 `spec.md`

至少包含：

```markdown
# Spec

## 背景

## 目标

## 非目标

## 当前行为

## 目标行为

## 功能需求

## 边界条件

## 技术约束

## 兼容性要求

## 验收标准

- [ ]
- [ ]

## 待确认问题
```

### 17.2 `plan.md`

描述如何实施已经确认的 Spec：涉及模块、修改顺序、数据流、接口变化、测试计划、风险、迁移策略、预计涉及文件。Plan 可以随着代码调研完善，但**不得绕过 Spec 改变需求本身**。

### 17.3 `acceptance.md`

```markdown
# Acceptance

## Result

PASS / FAIL

## Spec Coverage

## Tests

## Issues

## Regression Risks

## Required Rework
```

## 18. constitution/ 三件（仅 FULL 档）

### `mission.md`

记录长期稳定的信息：项目背景、项目使命、产品目标、核心用户价值、核心原则、长期方向、明确非目标。

### `roadmap.md`

记录路线与阶段目标，例如：

```text
Phase 0 — Prototype
Phase 1 — MVP
Phase 2 — Productization
Phase 3 — Expansion
```

Roadmap 描述阶段方向，不替代具体 Spec。

### `tech-stack.md`

记录已经确认的技术与工程约束：编程语言、框架、Runtime、包管理器、数据库、AI / LLM 服务、前后端架构、**业务代码目录**、测试框架、Build / Test / Lint / Format 命令、部署方式、外部依赖、重要技术限制。

业务代码目录一旦确定并投入使用，除非用户明确批准架构调整，Agent 不得擅自迁移或重命名。

## 19. 重要决策

重要决策写入**子项目内部**的 `.ai/decisions/`。

### 19.1 什么才算重要决策

**默认不写。** 同时满足以下三条才写，缺一条就不写：

1. **它是一个选择** —— 存在两个以上可行方案，或用户可能持不同意见的方向，
   不是某次修复的具体做法。
2. **难以从代码和现有文档反推** —— 未来的人读完代码、`constitution/`、Spec，
   仍不知道「为什么这么做、考虑过哪些替代方案」。
3. **约束未来** —— 改变的成本高（难以逆转），或对后续多个模块 / Spec 有约束力，
   或属于用户明确拍板 / 明确否决的方向。

写之前自问：**没有这条记录，未来的人会在哪里卡住？** 答不上来就不写。

架构方案、数据模型、API 行为、安全与权限、第三方服务、兼容策略、技术栈、
目录结构、用户明确否决的方案、需要偿还的技术债，都属于可写类型，
但**类型对号入座不等于可以写**，仍要先过上面三条。

### 19.2 不写的东西

- 验收通过（或失败）本身，以及验收 / 返工过程 —— 属于 `acceptance.md` 与 `rework.md`；
- 修 bug 的做法、调试过程、命令输出、测试结果；
- `constitution/`、`AGENTS.md`、Spec 已写清楚的结论的复述；
- 可以低成本随时改的局部实现选择；
- 顺着 Spec 正常实现的功能行为 —— 写进 Spec 就够了。

**验收通过不是写决策的理由；一轮返工也不自动产生决策。**

### 19.3 何时写、写多少

动手之前写。实施中被迫形成的新约束可在完成后立即补，只记结论与理由。
一个 Spec / Phase 通常 0–1 条。

需求变化进 Spec，阶段计划进 `roadmap.md`，工程约束进 `tech-stack.md`；
重要结论不能只存在于聊天上下文里。

推荐格式：

```markdown
# Decision

## Context

## Decision

## Reason

## Alternatives

## Consequences
```

## 20. 交接与报告

### 20.1 Task Handoff（协调 Agent → 子 Agent）

必须完整传递上下文，不得只发送一句「实现这个功能」。推荐结构：

```markdown
# Task Handoff

## Role

Implementation / Acceptance

## Goal

## Context

## Workflow Tier

LIGHT / FULL

## Project Mode

NEW / EXISTING

## Spec

## Relevant Files

## Scope

## Out of Scope

## Constraints

## Acceptance Criteria

## Commands

## Expected Output
```

交接内容必须让子 Agent 在不依赖聊天上下文的情况下理解任务。
**"完整"不等于"转述文件内容"**，只写三类东西：

1. **决策** —— 已经定了什么，为什么这么定；
2. **不可协商的约束** —— 硬规则、明确不做什么、范围边界；
3. **验收方式** —— 怎么算做完、需要什么证据，以及可复用资产在哪：
   已运行的服务与数据集、测试脚手架、一条命令可跑的烟雾套件、环境速查。

其余一律指路：项目规范、Spec、已有决策、现有代码，让子 Agent 自己读。
交接单越长，子 Agent 花在"执行清单"上的时间越多，花在"想清楚"上的时间越少。
**不要要求子 Agent「从零搭环境、从零写测试」**——独立的是判断与反例，不是脚手架。

### 20.2 Implementation Report（实施 Agent → 协调 Agent）

```markdown
# Implementation Report

## Summary

## Files Changed

## Important Decisions

## Tests

## Known Limitations

## Remaining Questions
```

存在无法执行的测试时，必须明确说明，**不得把「未执行」表述为「已通过」**。

### 20.3 Acceptance Report（验收 Agent → 协调 Agent）

验收必须独立于实施。验收内容包括：Spec 是否逐项满足；实现是否符合项目结构和技术约束；是否修改无关代码；是否存在边界遗漏；是否存在明显回归风险；测试是否充分；文档是否需要更新；是否违反目录结构规则；是否擅自改变业务行为。

结果只能是 `PASS` 或 `FAIL`。验收 Agent 发现问题时，应返回问题，**不得自己修改业务代码**。

### 20.4 返工

验收失败时：

1. 协调 Agent 读取完整 Acceptance Report；
2. 将失败原因整理为明确的返工目标；
3. 必要时创建或更新 `rework.md`；
4. 创建**新的**实施 Agent 进行返工；
5. 返工完成后创建**新的**独立验收 Agent；
6. 不得直接复用上一验收 Agent 的结论作为新一轮结果。

流程持续到：PASS，或用户明确接受已知问题，或用户取消任务。

---

# 第五部分　工程硬约束

## 21. 目录结构变更规则

目录结构属于高影响工程决策。

### 21.1 Agent 结构

以下结构未经用户明确授权，不得删除、移动、重命名或替代：

```text
<子项目>/AGENTS.md
<子项目>/constitution/
<子项目>/specs/
<子项目>/.ai/
```

工具站根目录的 `AGENTS.md`、`README.md`、`.gitignore` 同理。

### 21.2 工具站根目录

根目录只放三份文档和子项目文件夹（见第 2 节）。**不得**在根目录新建 `constitution/`、`specs/`、`.ai/`、`shared/`、`common/`、`packages/` 之类的目录 —— 除非用户明确批准全站结构调整。

**唯一已批准的例外：`.github/`（2026-09-16）**。GitHub Actions 的 workflow
只能位于仓库根目录，没有别的合法位置；因此 `.github/workflows/` 用于全站 CI/CD，
其内容属于共享设施，按第 5.3 节的原则由 `deploy/` 子项目在文档中说明与维护。
除此之外，新增任何根目录文件或目录仍需用户明确批准。

### 21.3 子项目内的项目结构

- NEW 项目：`docs/`、`tests/`、业务代码目录、Agent 结构一旦建立，后续允许扩展，但不得擅自删除、重命名、移动或重构为另一套目录体系。
- EXISTING 项目：原始目录默认视为既有工程约束，除非当前任务本身就是经用户批准的目录 / 架构重构，否则不得改变其位置和职责。

### 21.4 确实需要调整时

1. 说明为什么现有结构无法满足需求；
2. 明确影响范围；
3. 给出迁移方案；
4. 说明兼容和回滚风险；
5. 获得用户确认；
6. 写入对应子项目的 `.ai/decisions/`；
7. 再实施。

不得为了「更整洁」「更符合某个框架模板」「更符合个人习惯」「减少目录数量」「能用上自己熟悉的脚手架」而调整目录。

## 22. Git 与用户已有修改

开始修改前，应检查当前工作区状态。Agent 不得：

- 删除用户尚未提交的修改；
- revert 与当前任务无关的代码；
- 因为工作区不干净就擅自 reset；
- force checkout 覆盖未知修改；
- 把用户的修改误认为 Agent 自己产生的修改。

发现相关文件已经存在修改时：先阅读 → 判断修改来源和意图 → 尽可能兼容 → 存在不可安全合并的冲突时，由协调 Agent 处理并向用户说明。

## 23. 依赖与技术栈规则

必须优先遵循目标子项目的 `constitution/tech-stack.md`。不得无必要地：

- 更换语言、框架、包管理器；
- 删除 lockfile 或重建整个 lockfile；
- 升级大量无关依赖；
- 替换核心基础设施；
- 引入功能明显重复的依赖。

添加重要外部服务前，应判断：项目是否已有等价能力；是否产生费用；是否引入账号、密钥或平台绑定；是否形成重大架构依赖。

重大技术选择应记录到对应子项目的 `.ai/decisions/`。

## 24. 测试与验证规则

**不得仅凭代码阅读就宣布功能完成。**

验证优先级：

```text
任务相关测试
↓
模块测试
↓
类型检查
↓
Lint
↓
Build
↓
必要时更大范围测试
```

实际命令以目标子项目的 `constitution/tech-stack.md` 和真实配置为准。**不得凭空假设** `npm test`、`pytest`、`pnpm build` 一定可用，必须从项目真实配置中确认。

无法执行测试时，Implementation Report / Acceptance Report 必须说明：哪个测试没有运行、为什么、做了什么替代验证、剩余风险是什么。

LIGHT 档至少要做到：在浏览器或运行环境里真的把它跑一遍，确认主要功能可用，并在交付说明里写清楚验证了什么。

## 25. 文档规则

### 25.1 Agent 文档

以下内容发生重要变化时，应同步维护：`constitution/`、`specs/`、`.ai/decisions/`、`.ai/rules/`、`.ai/workflows/`。

### 25.2 项目文档

项目行为发生变化时，判断是否需要同步修改：README、API 文档、配置说明、Examples、部署指南、迁移指南，以及项目已有的其他文档。

新建 README 时保持结构清晰即可，不堆砌徽章。已有项目存在成熟 README 规范时，优先保持原风格，不因本规范强制重写。

### 25.3 全站文档

新增、删除或改名子项目时，同步更新：根 `README.md` 的工具清单、本文件末尾的「当前子项目」表。

## 26. 安全规则

Agent 不得：

- 输出或提交真实密钥；
- 把 `.env` 里的敏感信息写进日志；
- 把凭据硬编码到代码里；
- 擅自删除生产数据；
- 擅自操作生产环境；
- 擅自执行明显不可逆的操作；
- 在用户未确认的情况下创建明显会持续计费的云资源。

高风险动作包括：数据删除、Migration、Force Push、Reset、生产部署、权限变更、密钥轮换、调用外部收费 API、创建云资源、破坏性目录迁移。涉及高风险动作时必须额外确认。

---

# 第六部分　收尾

## 27. 上下文恢复

发生对话压缩、Agent 重启、用户中途发新消息、工作流意外中断、新 Agent 接手等情况时，**不得无依据地重新开始任务**。

首先检查：

1. 当前档位与项目状态（LIGHT / FULL、NEW / EXISTING）；
2. 根 `AGENTS.md`；
3. 目标子项目的 `AGENTS.md` 与 `constitution/`；
4. 当前 Spec、当前 Plan；
5. 已创建的子 Agent；
6. 已返回但未处理的报告；
7. `acceptance.md`、`rework.md`；
8. `.ai/decisions/`；
9. git diff 与当前工作区状态。

然后判断任务处于 Initialization / Planning / Implementation / Acceptance / Rework / Done 中的哪个阶段，从断点继续。

不得无依据地：重复创建同类 Agent、重复实施、覆盖已有修改、跳过已有报告、把普通用户追问理解为任务取消。**只有用户明确表示取消、停止或切换任务，才视为取消原任务。**

## 28. Definition of Done

任务只有在合理适用的以下条件满足后，才算完成：

- [ ] 已正确识别档位与项目状态；
- [ ] 没有破坏项目原有目录结构；
- [ ] Agent 结构符合本规范（FULL 档）；
- [ ] 用户目标已经实现；
- [ ] Spec 要求已经满足（FULL 档）；
- [ ] 没有明显遗漏，边界条件已考虑；
- [ ] 相关测试已经执行；
- [ ] 测试通过，或未执行项已明确说明；
- [ ] 必要的 Build / Type Check / Lint 已完成；
- [ ] 没有引入明显回归，没有修改无关代码；
- [ ] 重要决策已按第 19 节标准判断：有符合条件的则已记录，没有就明确说明没有；
- [ ] 相关文档已经同步；
- [ ] 多 Agent 模式下已完成独立验收；
- [ ] 所有子 Agent 结果已经读取和处理；
- [ ] 验收结果为 PASS，或用户明确接受剩余问题。

## 29. 最终交付规范

最终回复简洁说明：完成了什么；主要修改；验证（执行了什么、什么通过、
什么没有执行）；重要决策（如有）；剩余问题（如有）。
不得用过程日志代替结果。

## 30. 总原则

最重要的结构原则：

> **新子项目：按对应档位的标准结构开始，允许扩展，但不得擅自删除、移动或替换既有结构。**
>
> **已有子项目：尊重并保留原有结构，只把 Agent 结构接入，不得为了适配本规范而重构旧项目。**
>
> **工具站根目录：永远只有三份文档加 n 个子项目文件夹。**

最重要的开发原则：

> **先理解项目和需求，再通过 Spec、实施、独立验收与决策管理，以可验证、可追踪、可维护的方式完成修改。**

---

# 附录 A　子项目 AGENTS.md 模板（FULL 档）

```markdown
# AGENTS.md — <子项目名>

本文件是 <子项目名> 的项目说明，**继承工具站根目录 `AGENTS.md` 的全部通用规范**。
两者冲突时，以根目录规范为准；本文件只补充本项目特有的信息。

## 这是什么

<一句话说清这个工具解决什么问题>

## 档位

workflow_tier: FULL

## 项目结构

<本项目实际的目录结构，以及业务代码放在哪里>

## 技术栈摘要

<详见 constitution/tech-stack.md。这里只写最关键的几条>

## 本项目特有约束

<只属于本项目的规则；没有就写「无」>

## 常用命令

<install / dev / build / test 的真实命令>
```

# 附录 B　子项目 AGENTS.md 模板（LIGHT 档）

```markdown
# AGENTS.md — <子项目名>

本文件是 <子项目名> 的项目说明，**继承工具站根目录 `AGENTS.md` 的全部通用规范**。

## 这是什么

<一句话说清这个工具解决什么问题>

## 档位

workflow_tier: LIGHT

## 怎么跑

<直接打开某个 html 文件，或一条命令>

## 本项目特有约束

<没有就写「无」>
```

---

# 附录 C　新会话开场模板

**每个子项目单独开一个会话。** 新会话是彻底空白的，不知道工具站的任何事，
所以开场必须交代清楚三件事：**读哪份规范、目标是哪个子项目、这次要达成什么。**

照抄下面的模板，把尖括号里的内容换掉即可。

## C.1 新建一个子项目

```text
读一下 /Users/yang/project/Utility-Site/AGENTS.md（工具站总纲），然后按里面的规范跟我干活。

本次目标：新建子项目 <名字>（文件夹建议 <folder-name>）
它是做什么的：<一句话说清解决什么问题、给谁用>
这次要做到什么程度：<MVP / 先能跑起来 / 完整功能>
我还不确定的：<没想好的地方直说，让它来问你>

先别写代码。按总纲第 7 节的流程，先告诉我你对这个项目的理解、它该属于 LIGHT 档还是 FULL 档、
以及需要我确认的关键问题。我确认之后再动手。
```

## C.2 接手一个已经存在的子项目

```text
读一下 /Users/yang/project/Utility-Site/AGENTS.md，再读 <子项目文件夹>/ 里的全部内容。

本次目标：在 <子项目名> 上做 <具体需求>
先按总纲第 8 节，把这个子项目的现状、已有结构和约束梳理给我，不要改动任何文件。
```

## C.3 继续上一次没做完的活

```text
读一下 /Users/yang/project/Utility-Site/AGENTS.md，再读 <子项目文件夹>/AGENTS.md 和它的 specs/、.ai/decisions/。

本次目标：继续 <子项目名> 的 <spec-XXX-xxx>
按总纲第 27 节判断任务当前处于哪个阶段，从断点继续，不要重新开始。
先把你的判断和接下来的计划告诉我。
```

## C.4 三个要点

**明确点名根目录的 AGENTS.md** —— 不然新会话不知道读哪个。

**指名子项目，并说清是新建还是接手** —— 避免 `project_mode: AUTO` 误判成 EXISTING。

**要求它先复述理解、先给方案，别写代码** —— 把节奏握在自己手里。

## C.5 如果它说读不到文件

让它访问 `/Users/yang/project/Utility-Site` 即可。

---

# 当前状态

## 当前子项目

| 子项目 | 档位 | 一句话说明 | 状态 |
|---|---|---|---|
| [recording](./recording/) | FULL | 公司内部的语音样本采集：员工录中英文两段音频上传，管理员查看提交情况 | MVP 已完成 |
| [deploy](./deploy/) | LIGHT | 全站部署与运维：反向代理、端口分配表、部署脚本与 CI/CD（非业务工具） | 骨架 + 流水线已就位 |

## 全站待定

以下几件事还没有决定，**不要假设，也不要替用户决定**，需要时先问：

- **部署服务器与域名 / HTTPS**：架构已定（单服务器 + 每子项目一容器 + 共享反向代理，
  见 5.3），具体服务器、域名与证书来源尚未确定；CD 的服务器凭据也因此未配置；
- 是否将来真的需要共享代码层；
- 是否需要给所有子项目加一个统一的浏览器入口 / 索引页。

---

本文件的修改属于全站性变更，动手前先说明理由。
