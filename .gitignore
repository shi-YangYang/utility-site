# 工具站的忽略规则统一写在这里，子项目默认不建自己的 .gitignore。
# 只有某条规则仅对单个子项目成立、写进这里会误伤其他子项目时，
# 才在那个子项目内单独建一份并注明原因。详见 AGENTS.md 第 5.1 节。

# 系统 / 编辑器
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# 依赖
node_modules/

# 构建产物
dist/
build/
*.log

# 环境变量与密钥 —— 永远不要提交
.env
.env.*
# 例外：配置样例（.env.example）里只有变量名和占位值，没有真实密钥，是要提交的。
# 写成不带路径前缀的形式，对任意深度的子项目都生效。
!.env.example
*.pem

# 部署：服务器上由 Caddyfile.example 复制出的真实配置（含服务器地址），不进仓库
deploy/proxy/Caddyfile
