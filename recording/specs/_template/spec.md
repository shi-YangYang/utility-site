# Spec — <名称>

> 复制本文件到 `specs/spec-XXX-short-name/spec.md` 后填写。方括号内容是提示，填完删掉。

## 背景

[为什么现在要做这件事。现状是什么，痛点是什么。]

## 目标

[这次要达成什么。一两句话，可验证。]

## 非目标

[明确不做什么。这一节和「目标」同等重要——它划定实施边界。]

## 当前行为

[改动前的系统行为。新项目写「无，本项目为新建」。]

## 目标行为

[改动后的系统行为。描述用户能感知到的变化，不是代码怎么改。]

## 功能需求

[逐条列出。每条应可独立验证。接口写清路径、方法、请求、响应、错误。]

## 边界条件

[异常输入、并发、重复操作、超时、缺文件、权限不足……]

## 技术约束

[必须遵守的技术限制。与 constitution/tech-stack.md 呼应，不重复抄写。]

## 兼容性要求

[是否需要兼容既有数据、既有调用方、既有浏览器。]

## 验收标准

- [ ] [可勾选、可验证的标准]
- [ ]

## 待确认问题

[尚未决定、需要用户拍板的问题。没有就写「无」。]