# 本项目为什么单独建一份 .gitignore，而不是把规则加到根目录：
# `data/` 是 recording 独有的运行时目录，里面全是录音文件，绝不能提交。
# 但 "data/" 这种不带路径前缀的写法写进根 .gitignore 会匹配任意深度的同名目录，
# 可能误伤其他子项目里恰好叫 data/ 的源码目录。
# 按根 AGENTS.md 第 5.1 节，规则仅对单个子项目成立时，才在子项目内单独建。
#
# 注意：这是子项目局部规则，不要在这里重复根 .gitignore 已经覆盖的通用规则。

data/