# .ai/rules/

本项目特有的、比 `AGENTS.md` 更细的开发规则。

判据：一条规则如果只对 `recording` 成立、且细到不适合放进 `AGENTS.md` 的
「本项目特有约束」，就写在这里。

当前（Phase 1 MVP）的硬约束都写在 `AGENTS.md` 与
`constitution/tech-stack.md` 里，本目录暂时为空。

（这个文件的作用是让空目录能被 git 跟踪。）