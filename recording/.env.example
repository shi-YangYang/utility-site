# recording 的配置样例。
#
# 用法：复制成 .env（同一目录下），按需修改，然后 npm start。
#   cp .env.example .env
#
# 说明：
#   - .env 不会被提交（根 .gitignore 已忽略），可以放心写真实口令；
#   - 命令行上显式传的环境变量优先级高于 .env；
#   - 这里列出的都是可选项，不配置就用下面的默认值。

# 监听端口
PORT=3000

# 员工端入门口令。员工凭它进入录音页面。
EMPLOYEE_CODE=123

# 管理端口令。**上线前必须改掉**，仍是 admin123 时服务启动会打印警告。
ADMIN_CODE=admin123

# 数据目录：索引与录音都放在这里。
# 相对路径以项目根目录为基准；Docker 部署时指向容器内可挂载的路径。
DATA_DIR=./data

# 转码后的 WAV 采样率（Hz）。单声道、16-bit PCM。
# 默认 16000 是语音分析的通用标准；下游若有更高要求，改这一个值即可。
SAMPLE_RATE=16000

# 单个上传音频文件的大小上限（MB）
MAX_UPLOAD_MB=50

# ffmpeg 可执行文件路径。默认走 PATH；不在 PATH 里时填绝对路径。
# ffprobe 会自动取同目录下的同名程序。
FFMPEG_PATH=ffmpeg