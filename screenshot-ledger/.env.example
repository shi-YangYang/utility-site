# 复制本文件为 .env 并填入真实值。
# .env 不会进入仓库（根 .gitignore 覆盖），本文件是唯一的配置样例。
#
# 【重要】EXPO_PUBLIC_ 前缀的变量会被编译进 App 包（APK），
# 任何拿到安装包的人都能提取出来 —— 这是有意接受的设计：
# 只分发给可信的同事、单独申请一把 Key 并设置额度上限；
# 一旦泄露，只能在服务商侧轮换 Key 并重新打包。

# ── Base URL ────────────────────────────────────────────────────────────
# 阿里云百炼的 Base URL 与 Key 严格配套，混用会直接 401：
#   按量付费（通用 Key，sk- 开头）：
#     https://dashscope.aliyuncs.com/compatible-mode/v1
#     （也可用业务空间专属域名：https://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/compatible-mode/v1，
#       WorkspaceId 在百炼控制台「业务空间详情」里看）
#   Token Plan（套餐 Key，sk-sp- 开头）：
#     https://token-plan.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
#     注意：官方条款写明 Token Plan「仅限在兼容的 AI 编程和智能体工具中交互式使用，
#     不可用于自动化脚本或应用后端」——本 App 属于应用后端调用，使用前请自行评估风险。
#   其他服务商：填它的 OpenAI 兼容地址即可。
EXPO_PUBLIC_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# ── 模型 ────────────────────────────────────────────────────────────────
# 必须是能看图的多模态模型（纯文本模型读不了截图）。
# 百炼示例：qwen3.7-plus（文本 + 图片 + 视频输入，视觉能力已并入 Plus 系列）；也可用 Qwen-VL 系列。
EXPO_PUBLIC_LLM_MODEL=qwen3.7-plus

# ── API Key ─────────────────────────────────────────────────────────────
# 占位值，不要提交真实值。Key 的前缀必须与上面的 Base URL 配套（见上）。
EXPO_PUBLIC_LLM_API_KEY=sk-REPLACE_ME
